spring:
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
        username: nacos
        password: nacos
        namespace: gateway
  application:
    name: gateway-server-app
  profiles:
    active: simple
server:
  port: 8888
